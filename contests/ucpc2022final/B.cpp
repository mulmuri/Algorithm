#include <iostream>
#include <algorithm>
#include <string>
#include <vector>

int readInt(const std::string& code, int& index) {
    
}

struct Node {
    int operatorId;
    std::vector<int> operands;

    Node(const std::string& code, int& index) {
        
    }
};

int main() {
    std::cin.tie(nullptr)->sync_with_stdio(false);
    
    int n;
    std::cin >> n;
    std::string code;
    std::cin >> code;

    

    return 0;
}